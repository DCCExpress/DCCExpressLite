# DCCExpressLite starter data

This optional installer package contains a small sample layout, four example locomotives, and their matching PNG images.

The locomotive DCC addresses are examples. Review and edit them before operating a real layout. Installing this package replaces the current layout and locomotive list on the EX-CSB1; the installer keeps this option mutually exclusive with restoring an existing backup.
